[**NeuroLink API Reference**](../README.md)

---

[NeuroLink API Reference](../README.md) / BankedArtifactKind

# Type Alias: BankedArtifactKind

> **BankedArtifactKind** = `"worker-report"` \| `"command-output"` \| `"stage-output"` \| `"other"`

What a banked payload is, so previews and logs can say so without the
caller re-explaining itself. "other" is the escape hatch, not the default.
